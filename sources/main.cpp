#include "Statistics.hpp"

int main(){
    // League myLeague = new League();
    // Schedule mySchedule = new Schedule(myLeague);
    
    // while (mySchedule.gamesLeft() != 0)
    // {
    //     Game * currGame = mySchedule.getGame();
    //     currGame->playGame();
    //     mySchedule.nextGame();
    // }
    // Statistics myStatistics = new Statistics(myLeague);
    // cout << "Team Positions:  " << endl;
    // vector<Team *> points = myStatistics.points();
    // for(unsigned int i=0; i< points.size();i++){
    //     cout << i << ": " << points.at(i)->getName << endl;
    // }
    // cout << " Longest Win streak in this season is: " << myStatistics.longestWinStreak();

    // cout << " Longest Lose streak in this season is: " << myStatistics.longetLoseStreak();

    // cout << " Number of teams that scored more than they recived : " << myStatistics.moreScoredThenRecived();
    
    
}